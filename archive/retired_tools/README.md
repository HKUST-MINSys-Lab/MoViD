# Retired Tools

This directory keeps older non-core helpers that are no longer part of the active MoViD tree but are still useful to preserve in version control.

Current archived groups:

- `analysis/`: older analysis and visualization scripts
- `research/`: experimental prototypes
- `data/`: one-off dataset conversion helpers that are not part of the active preprocessing workflow
