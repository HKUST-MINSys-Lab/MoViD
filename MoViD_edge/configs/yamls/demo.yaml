LOGDIR: ''
DEVICE: 'cuda'
EXP_NAME: 'demo'
OUTPUT_DIR: 'experiments/'
NUM_WORKERS: 0
MODEL_CONFIG: 'configs/yamls/model_base.yaml'
FLIP_EVAL: True

TRAIN:
  STAGE: 'stage2'
  CHECKPOINT: 'checkpoints/movid_stage1.tar.pth'
MODEL:
  BACKBONE: 'vit'
