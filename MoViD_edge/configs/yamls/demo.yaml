LOGDIR: ''
DEVICE: 'cuda'
EXP_NAME: 'demo'
OUTPUT_DIR: 'experiments/'
NUM_WORKERS: 0
MODEL_CONFIG: 'configs/yamls/model_base.yaml'
FLIP_EVAL: True

TRAIN:
  STAGE: 'stage2'
  #CHECKPOINT: 'checkpoints/checkpoint.pth copy.tar'
  #CHECKPOINT: '/home/dlq/MoViD/checkpoints/model_best_wo_feature.pth.tar'
  CHECKPOINT: '/home/dlq/MoViD/checkpoints/model_best.pth.tar'
MODEL:
  BACKBONE: 'vit'