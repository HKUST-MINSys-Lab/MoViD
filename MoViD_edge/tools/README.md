# Tools

This directory contains non-core utilities that support edge-side inference.

- `inference/`: the active streaming helper used by `real_time.py`
- `model/`: action-model download and TensorRT conversion tools

The active edge runtime entrypoints remain at the repository root:

- `real_time.py`
- `demo.py`
